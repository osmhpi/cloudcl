apply plugin: 'java'
apply plugin: 'eclipse'

version = '0.0.1'

repositories {
    mavenCentral()
}

buildDependents.dependsOn { subprojects.build }
build.dependsOn { copyRuntimeLibs }

sourceSets {
    main {
        java {
            srcDirs = ["src","samples"]
        }
    }

    test {
        java {
            srcDirs = ["test"]
        }
    }
}

test {
   testLogging.showStandardStreams = true
 
  beforeTest { descriptor ->
     logger.lifecycle("Running test: " + descriptor)
  }

  onOutput { descriptor, event ->
     logger.lifecycle("Test: " + descriptor + " produced standard out/err: " + event.message )
  }
}

dependencies {
	implementation group: 'com.amazonaws', name: 'aws-java-sdk-ec2', version: '1.11.34'
	implementation group: 'log4j', name: 'log4j', version: '1.2.17'
	implementation files('aparapi/com.amd.aparapi/dist/aparapi.jar')
	testImplementation group: 'junit', name: 'junit', version: '4.12'
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}

jar {
    manifest {
        attributes 'Implementation-Title': 'DynamOpenCL',
                   'Implementation-Version': archiveVersion
    }
}

task copyRuntimeLibs(type: Copy) {
	into new File(project.buildDir, "/libs/lib/")
	
	from configurations.runtimeClasspath

}